// isko banate hai jab humare pass jada components ho jaye to hum usse bar bar import na kare ye direct isko kar ke use kar le 

import InputBox from "./InputBox";

export {InputBox}

// like this way if we want to addd some etc just like this {InputBox,Button, etc...}